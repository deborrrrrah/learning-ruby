<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Food Order Management System</title>
  <link rel="stylesheet" href='/stylesheets/index.css'>
</head>
<body>
  <div class="container">
    <div class="col"></div>
    <div class="body">
      <div class="menu-bar">
        <ul>
          <li><a href="/items" class="router">Menu Items</a></li>
          <li><a href="/categories" class="router">Categories</a></li>
          <li><a href="/orders" class="router">Orders</a></li>
          <li><a href="/customers" class="router">Customers</a></li>
          <li><a href="/cart" class="active"><img src="/img/cart.png" alt="Cart" width="20" height="20"></a></li>
        </ul>
      </div>
      <div class="header">
        <div class="header-title">
          <h1>Cart</h1> 
        </div>
      </div>
      <form method="POST" action="/cart/submit">
        <div class="content roboto black">
          <label for="items">Items</label>
          <br>
          <br>
          <table id="table">
            <tr>
              <th>Name</th>
              <th>Category</th>
              <th>Price</th>
              <th>Action</th>
            </tr>
            <% cart.items.each do |item| %>
              <tr>
                <td><%= item[:item].name %></td>
                <td><%= item[:item].categories_to_s %></td>
                <td><%= item[:item].price %></td>
                <td>
                <input type="number" id="<%= item[:item].id %>" name="<%= item[:item].id %>" min="1" value=1>
                <a href="/cart/delete/<%= item[:item].id %>" class="router margin-right"><img src="/img/delete.png" alt="Delete" width="20" height="20"></a>
                </td>
              </tr>
            <% end %>
          </table>
          <label for="customer_id">Customer</label>
          <select name="customer_id" id="customer_id">
            <% customers.each do |customer| %>
              <option value=<%= customer.id %>><%= customer.name %></option>
            <% end %>
          </select>
        </div>
        <a href="/cart/remove-all" class="router">Remove All</a>
        <br>
        <input type="submit" value="Submit Order">
      </form>
      </div>
    </div>
    <div class="col"></div>
  </div>
</body>
</html>