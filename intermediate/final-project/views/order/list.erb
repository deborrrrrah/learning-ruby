<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Food Order Management System</title>
  <link rel="stylesheet" href='/stylesheets/index.css'>
</head>
<body>
  <div class="container">
    <div class="col"></div>
    <div class="body">
      <div class="menu-bar">
        <ul>
          <li><a href="/items" class="router">Menu Items</a></li>
          <li><a href="/categories" class="router">Categories</a></li>
          <li><a href="/orders" class="active">Orders</a></li>
          <li><a href="/customers" class="router">Customers</a></li>
          <li><a href="/cart" class="router"><img src="/img/cart.png" alt="Cart" width="20" height="20"></a></li>
        </ul>
      </div>
      <div class="header">
        <div class="header-title">
          <h1>Order List</h1> 
        </div>
      </div>
      <div class="content roboto black table-div">
        <table id="table">
          <tr>
            <th>Customer</th>
            <th>Item</th>
            <th>Date</th>
            <th>Action</th>
          </tr>
          <% orders.each do |order| %>
            <tr>
              <td><%= order.customer.name %></td>
              <td><%= order.items_to_s %></td>
              <td><%= order.order_date_time %></td>
              <td>
                <a href="/orders/detail/<%= order.id %>" class="router margin-right"><img src="/img/detail.png" alt="Detail" width="20" height="20"></a>
              </td>
            </tr>
          <% end %>
        </table>
        <br>
        <div class="pagination">
          <% 1.upto(max_page) do |page_idx| %>
            <% if page_idx == page %>
              <a class="active" href="/orders/page/<%= page_idx %>"><%= page_idx %></a>
            <% else %>
              <a href="/orders/page/<%= page_idx %>"><%= page_idx %></a>
            <% end %>
          <% end %>
        </div>
      </div>
    </div>
    <div class="col"></div>
  </div>
</body>